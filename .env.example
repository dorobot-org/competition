# ============================================
# GPU Portal - Environment Configuration
# ============================================
# Copy this file to .env and fill in your actual values
# NEVER commit the .env file to git

# --------------------------------------------
# JWT Configuration (REQUIRED)
# --------------------------------------------
# Secret key for signing JWT tokens
# CRITICAL: Generate a new secure random key for production
# Generate with: python -c "import secrets; print(secrets.token_urlsafe(64))"
JWT_SECRET_KEY=CHANGE_ME_GENERATE_NEW_SECRET_WITH_PYTHON_COMMAND_ABOVE

# --------------------------------------------
# Admin Configuration (REQUIRED - First Run Only)
# --------------------------------------------
# Initial admin password - ONLY used when creating admin user for the first time
# After initial setup, change the password through the web interface
# Admin login: Phone: 13800000000, Password: [value below]
# IMPORTANT: Remove this variable after initial setup to prevent password resets
ADMIN_INITIAL_PASSWORD=CHANGE_ME_TO_SECURE_PASSWORD

# --------------------------------------------
# GPUFree API Configuration (REQUIRED)
# --------------------------------------------
# Bearer token for GPUFree API authentication
# Obtain from: https://www.gpufree.cn (check browser network tab when logged in)
GPUFREE_BEARER_TOKEN=your-gpufree-bearer-token-here

# --------------------------------------------
# Database Configuration (Optional)
# --------------------------------------------
# Path to SQLite database file
# Default: /data/portal.db (inside Docker container)
DATABASE_PATH=/data/portal.db

# --------------------------------------------
# CORS Configuration (Optional)
# --------------------------------------------
# Comma-separated list of allowed origins for CORS
# Development default: http://localhost:5173,http://127.0.0.1:5173
# Production: Set to your actual frontend domain
CORS_ALLOWED_ORIGINS=http://localhost:5173,http://127.0.0.1:5173

# --------------------------------------------
# Logging Configuration (Optional)
# --------------------------------------------
# Log level: DEBUG, INFO, WARNING, ERROR, CRITICAL
LOG_LEVEL=INFO

# ============================================
# SECURITY CHECKLIST
# ============================================
# [ ] Generated new JWT_SECRET_KEY with secure random value
# [ ] Set strong ADMIN_INITIAL_PASSWORD (12+ chars, mixed case, numbers, symbols)
# [ ] Obtained real GPUFREE_BEARER_TOKEN from gpufree.cn
# [ ] Set CORS_ALLOWED_ORIGINS to production domain (not localhost)
# [ ] Verified .env is in .gitignore
# [ ] Plan to remove ADMIN_INITIAL_PASSWORD after first run
